# Clean Architecture, DDD & CQRS

Status: **Not started**

## Planned coverage
- Layered architecture vs Clean/Onion/Hexagonal architecture — dependency direction diagrams
- Domain-Driven Design essentials: entities, value objects, aggregates & aggregate roots, domain events, repositories, bounded contexts
- Anemic vs rich domain models
- CQRS: separating command/query responsibilities, when it's worth the complexity (and when it's overkill)
- MediatR: pipeline behaviors (validation, logging, transactions as cross-cutting concerns) — request/response diagram
- Application layer vs Domain layer vs Infrastructure layer responsibilities
- Ports & Adapters — how this connects to [[11-Dependency-Injection]] and [[08-SOLID-Principles]] (Dependency Inversion in practice)
- When NOT to use Clean Architecture/DDD (CRUD apps, small services) — interviewers value knowing the tradeoffs, not just the pattern
