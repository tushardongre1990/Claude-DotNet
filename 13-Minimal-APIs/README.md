# Minimal APIs

Status: **Not started**

## Planned coverage
- `app.MapGet/MapPost/...` — how routing works without controllers
- Route groups (`MapGroup`), endpoint filters
- Parameter binding in minimal APIs (`[FromBody]`, `[FromServices]`, `[AsParameters]`)
- Minimal API vs Controller-based API — tradeoffs, when interviewers expect you to pick one
- Performance differences (less overhead, no filter pipeline by default)
- OpenAPI/Swagger integration with minimal APIs
